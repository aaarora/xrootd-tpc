apiVersion: v1
kind: Service
metadata:
  name: tpc-ucsd
  namespace: osg-services
  labels:
    run: tpc-ucsd
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    k8s-app: tpc-ucsd
  type: ClusterIP
