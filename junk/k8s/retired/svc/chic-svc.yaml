apiVersion: v1
kind: Service
metadata:
  name: tpc-chic
  namespace: osg-services
  labels:
    run: tpc-chic
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    k8s-app: tpc-chic
  type: ClusterIP
